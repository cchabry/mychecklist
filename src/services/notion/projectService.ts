
/**
 * Re-export du service de projets (pour compatibilité avec le code existant)
 * @deprecated Utilisez plutôt l'import depuis './project/projectService'
 */

import { projectService } from './project/projectService';

export { projectService };
export default projectService;
