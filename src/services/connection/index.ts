
/**
 * Service de gestion du mode de connexion
 * Remplace le système operationMode avec une approche simplifiée
 */

// Exporter les types
export * from './types';

// Exporter le service
export { connectionModeService } from './connectionModeService';

// Note: Le hook useConnectionMode est importé depuis @/hooks/useConnectionMode
